# Hugging Face access token.
# Store the real value in .env locally or Colab Secrets; never here.
HF_TOKEN=

# Fine-grained read-only access to this private GitHub repository for Colab.
# Store the real value in Colab Secrets or .env; never commit it.
GITHUB_TOKEN=

# Optional experiment tracking.
WANDB_API_KEY=
WANDB_MODE=offline
